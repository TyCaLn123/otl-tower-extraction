[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "otl-tower-extraction"
version = "0.1.0"
description = "Shape Prior-Guided Coarse-to-Fine Extraction of Overhead Transmission Line Towers from UAV LiDAR Point Clouds"
requires-python = ">=3.9"
